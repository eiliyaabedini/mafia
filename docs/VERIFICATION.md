# Prototype verification

## Speaker-colored conversation cards — 2026-09-13

- Public speech, defense, and skip cards now use the speaking character's profile color for a `1.5dp` border as well as the existing name label. Human and AI cards keep their distinct dark fills; controlled alpha preserves contrast while making consecutive speakers easier to identify.
- Kotlin/Wasm and Kotlin/JS production distributions passed. The 103-file Vercel package is 41.95 MiB with fingerprint `e9f3bb79b42b1588ff11c6f9cafb3dc617942824a17cafa22fbdc9361dca0ed4`. Production deployment `dpl_CY2eGES8X3GocqaP4piDPmCx7o9P` is Ready on all three aliases, and its public app bundle and changed Wasm executable match the prepared output byte-for-byte.

## Vercel Web Analytics — 2026-09-13

- Added Vercel's framework-independent HTML loader for `@vercel/analytics` 2.0.1 to the static entry page. It requests the deployment-owned `/_vercel/insights/script.js` route and automatically counts anonymous visitors and page views after deployment.
- No custom event API is called. Game state, player names, dialogue, hidden roles, wallet balance, model output, and local saves never enter the Analytics integration.
- Production deployment `dpl_4Q28hvBm5A8yZ4Dr3f5QU4n9xaTR` is Ready with package fingerprint `30a9aa2007c74efb53308aef93694c154efc263de8348c5965260a5a052ffb80`. The public entry, bridge, app bundle, and client configuration match the prepared package. The live Analytics script route returns JavaScript on `mafiai.vercel.app`; after desktop and Android production visits, the refreshed Vercel dashboard showed 3 visitors and 3 page views instead of the setup screen.

## Mobile AI Pass login continuity — 2026-09-13

- The header chip opens the official SDK connection surface on signed-out desktop and mobile browsers. Mobile detection includes ordinary phone/tablet user agents, touch-mode iPads, and compact coarse-pointer browsers; the SDK uses a redirect only after the user chooses sign-in in its surface.
- The bridge warms the public SDK/configuration during the larger Wasm load. It starts `walletOpen` in the next browser task because Compose canvas taps can otherwise synthesize a DOM click after the modal is inserted and immediately dismiss its backdrop. A bounded non-billable retry covers a transient cold mobile surface failure.
- Before a redirect from an active game, the bridge stores only that game's opaque local ID in both session and local storage. A fresh callback containing both OAuth `code` and `state` consumes and clears both markers within 15 minutes. If the ID matches the locally saved game, the controller is constructed with that table already open and paused before its first UI frame; onboarding is suppressed and no model or TTS call starts automatically. Lobby login has no game marker and stays in the lobby.
- The owner-approved OAuth migration provisioned public client `client_Yj314ILBgZcxgQlPYRkoYA` with localhost plus all three stable Vercel aliases. Mobile OAuth can therefore return to the exact origin that launched it. JavaScript syntax and both Kotlin web production targets passed.
- A fresh Android 16 Chrome session loaded the published lobby. One ordinary short tap on the Persian AI Pass credit control opened the SDK-owned `Connect your AI Pass` modal in 100 ms and left it open; the login control was not pressed. The bridge harness also verified next-task wallet opening, a bounded transient retry, duplicate resume markers, exact game-ID consumption, and marker cleanup. Account entry remains deliberately excluded from automated verification; the final authenticated return requires a real AI Pass login.
- The final 103-file package is 41.95 MiB with fingerprint `201528e7576554858507929cfd6e0f413fc4f03787e76aa5fe2d9b62019c93df`. Production deployment `dpl_Ar1QCjMg6bgFeWrU8jg5JWAmQC1Q` is Ready on all three aliases. Public `index.html`, `ai-bridge.js`, `styles.css`, `webApp.js`, `aipass-config.json`, and `sw.js` match the prepared output byte-for-byte. No account login, model request, or TTS request was made.

## Gameplay TTS model pin — 2026-09-13

- Corrected the live-game speech catalog preference, which had placed `tts-1` ahead of `gpt-4o-mini-tts` and therefore selected `tts-1` whenever both were available. Gameplay narration is now pinned to `gpt-4o-mini-tts`; a missing catalog entry produces the existing visible audio failure instead of silently charging a different speech model. Prerecorded tutorial narration remains unchanged and local.
- Live dialogue requests use the model-supported `1.25×` speed instead of `1.12×`. Mina now uses the distinctly feminine `coral` voice instead of the more neutral `alloy`; the other five character voices remain stable.
- The updated 103-file package is 41.95 MiB with fingerprint `a6612eea5af2338f9912658fc4c6c19f10e32d1e3154c41fc47a1febe7df17d3`. Production deployment `dpl_3sS2etK9UKnzdaPssCPRyGpUbF7Q` is Ready on all three aliases. The public bridge matches the package byte-for-byte and contains `SPEECH_SPEED = 1.25` plus Mina's `coral` mapping. No paid TTS request was made.
- Bridge syntax and the Kotlin/Wasm production build passed. The 103-file Vercel package has fingerprint `5a4baa05dc6d88ffd934c17fa85f0890a681950394d4d128a31a027521abc4a4`; deployment `dpl_2DgFUxJw8exmsaKf9Qkz9kE6Dmbm` is Ready on all three aliases. The public bridge matches the prepared file byte-for-byte, contains only `gpt-4o-mini-tts` in its gameplay preference, and has no `tts-1` literal. No paid TTS request was made.

## Ballot continuity, anchored chat and spectator pacing — 2026-09-13

- Removed the separate full-screen “last vote” overlay. When the final person completes a nomination candidate or final ballot, the authoritative engine result is saved immediately while Compose keeps the original voting screen visible with that last public vote for 2.8 seconds; elimination and night announcements wait until the hold finishes.
- Reworked conversation following around explicit reader intent. A reader at the live edge stays pinned as messages arrive. Once they scroll into history, stable item keys and disabled auto-follow preserve that position; the latest-messages button explicitly returns to the bottom.
- Added text-length reading time after every AI speech when TTS is not playing, with a 3–7 second spectator range after the human is eliminated. Successful TTS playback supplies the reading time and receives a short pause before the next turn. `?debug=1` skips these artificial delays and the ballot hold without changing request, timeout, or audio behavior.
- Kotlin/Wasm and Kotlin/JS production compilation, bridge syntax, and Vercel packaging passed. The final package contains 103 static files / 41.95 MiB with fingerprint `4f2d15eb25be04f59394ac39df3ae854b8221e4de4f55f9971834a6deeb8c4aa`. Production deployment `dpl_BcrwxjB5S3jy5m1FMw1HPPp9azb4` is Ready on all three aliases; public `index.html`, `webApp.js`, and `ai-bridge.js` match the prepared output byte-for-byte. A fresh production tab loaded the lobby and existing saved-game offer. No model or TTS request was made for this verification.

## AI Pass account chip and SDK wallet — 2026-09-13

- Replaced the Persian `اعتبار` box with a compact left-to-right AI Pass brand chip. Signed-out sessions show `AI CONNECT`; connected sessions show `AI PASS` and the balance as an English dollar amount rounded to the same two-decimal presentation used by the official widget. Unknown and loading states never invent a zero balance, and the last known amount remains visually muted when stale.
- The chip now calls the official SDK's public `openWallet()` method. In a local signed-out browser, clicking it opened the official “Connect your AI Pass” surface. On the authenticated production origin, the chip displayed `$3.49`, and clicking it opened the SDK-owned account dialog with the matching balance, add-funds buttons, gift-card redemption, Dashboard, and Disconnect. The app does not receive credentials or implement payment controls itself.
- Removed the app's duplicate balance/refresh dialog and the large explanatory footer card. The save/backup action remains available as a small link beside the player name; account disconnection remains in the official SDK dialog.
- `node --check` passed for the bridge and the Kotlin/Wasm production distribution built successfully. After removing the obsolete custom logout route and strings, the final Vercel package contains 103 static files / 43,991,523 bytes with fingerprint `d681b473f09ed1b111263840a3a8fbc506c50f88786c40356c4c8be6b83eeb54`. Production deployment `dpl_3FjUSGZ4QJsgKRzDwibCeUThA1fk` is Ready on all three project aliases, and the public `webApp.js` and `ai-bridge.js` match the prepared output byte-for-byte. No generation or TTS request was made during these wallet checks.

## Beginner night tutorial and ordered night turns — 2026-09-13

- Expanded the beginner track from five to six cards. The role card now explains what all four roles do, and the new card immediately before the exercises teaches the complete hidden night sequence: living Mafia choose a Town target, the Doctor protects one living player, and the Detective checks one other player and privately receives “Mafia” or “Town member.” It also explains why morning may reveal one death or no death. The existing no-death question therefore tests a rule already taught instead of introducing it inside the quiz.
- The deterministic engine now uses the same explicit Mafia → Doctor → Detective actor order. Each role still receives only its own legal action and private information. Restored games accept any valid partial set of night actions, so a save made during the older seating-order implementation is not discarded merely because its completed actors are not a prefix of the new order.
- Regenerated only `basic_roles` and the new `basic_night` narration with AI Pass `gemini-3.1-flash-tts` and `Charon`: two paid requests covering 583 spoken characters. The MP3s are 25.12 and 27.72 seconds. The other 39 clips were reused. The complete local bundle now contains 41 clips / 6,817 spoken characters / 639.120249 seconds / 10,271,047 bytes, and every clip passes the manifest, hash, decode, mono, 44.1 kHz, duration, and size checks.
- The Kotlin/Wasm production build and Vercel package passed with 103 static files / 44,004,183 bytes and fingerprint `689fe6dae63a32e639108719e2084985b76fc421a178c181394e8c5b18308341`. Production deployment `dpl_37sph3Gku7iyiw8dw3UMqpvpztrf` is Ready on all three project aliases. Public `webApp.js`, `ai-bridge.js`, `basic_roles.mp3`, and `basic_night.mp3` match the prepared package byte-for-byte; both narration files return `audio/mpeg`.
- A fresh production browser opened the six-card Persian beginner track, rendered the new card as step 5 of 6, and showed active local narration after a pointer gesture. The visible text contained the exact night order, Doctor save outcome, possible Mafia kill, and private Detective result. No live-game model request was started.

## UI copy cleanup — 2026-09-13

- Removed the recurring free/no-wallet/cost explanatory copy from the welcome, tutorial header, idle narration control, tutorial completion, lobby, wallet details, audio settings, music settings, effects settings, leave confirmation, and retry error card. No replacement marketing copy was added. Functional balance labels and authentication/insufficient-balance errors remain.
- Updated the tutorial manifest and regenerated only `complete_basics` with AI Pass `gemini-3.1-flash-tts` and `Charon`: one paid 83-character request produced a 7.20-second mono 44.1 kHz MP3. At that deployment, the 40-clip bundle was 610.280 seconds / 9,808,323 bytes and all clips passed the existing manifest, hash, decode, duration, and size checks.
- The production Wasm build and Vercel packaging passed: 102 static files / 43,538,565 bytes, fingerprint `3ff29d4631e9a0b395f55867e02039c10277b58b633e490b5be07be97f076ff2`. Local browser review covered the welcome, tutorial header, stopped narration control, audio dialog, wallet dialog, and lobby. Production deployment `dpl_SapQJPmju9Xyq5wiZn7UYY5t6jpj` is Ready on both stable aliases; the public application bundle and replacement narration match the prepared package byte-for-byte.

## Names, saves, sound effects and two-stage voting — 2026-09-13

- **Completed a real wallet-funded game on localhost through a Day 3 Town victory**, starting from random role assignment as the Doctor. The final saved game is `game--1563347905`: 65 chat requests, 74 public nomination decisions (27 yes / 47 no), 16 final decisions (15 votes / 1 abstention), eight defense speeches including a human defense, three daytime eliminations, one night kill and one successful Doctor save. The Doctor legally protected self on both nights. Roles revealed only on the finished result screen, and the saved winner matches the engine. The two failed Haiku attempts are included in the 65 requests; no further format errors or request timeouts occurred after the repair. Console error inspection at completion was empty.
- **Published to the existing stable Vercel site** as `dpl_9DCae4xkmv6AofWvrKQy5LQJA46f`. Final package fingerprint: `4fd4dc4505e5a4640bdd78c96c5982d4535537b95929b4a939d95890af50396f`. All 97 static production files / 28,901,961 bytes were packaged successfully. Root HTML, the bridge, and representative welcome and multi-voice MP3s fetched from the stable alias matched the prepared build exactly; MP3 responses use `audio/mpeg`. A fresh production browser tab loaded the five-card Persian beginner track and showed active local playback for both the welcome and the first multi-voice practice scene. No live-game model request was started during this production review.
- All 65 recorded request costs were unknown (`estimatedCost=null`), **not zero**. Token/model usage and the live wallet balance were available. The current AI Pass SDK reports authoritative charges through separate settlement APIs; exact per-request settlement is not wired into this game's usage entries. No full-game price is inferred from a shared wallet balance or presented as free; TTS costs are also outside the chat usage list.
- Added candidate-by-candidate yes/no nominations, including multiple or zero approvals. At most two positive leaders defend; a tie producing three or more finalists ends the day without defense or elimination. After one defense per valid finalist, each living player gets one final target or abstains. A final tie or all abstaining eliminates nobody. Saved legacy single-vote rounds and already-saved multi-finalist defenses retain their existing choices and complete normally; their next discussion uses the new limit.
- AI nomination plans remain private and cost one model request per AI, then every yes/no is revealed in public voter order. Defenses and final ballots are fresh decisions. Context includes public nomination votes, final votes/abstentions, departures, speaking order, and whether each player has had a public turn today. It excludes unrevealed plans and other players' private night information. A missing Mafia attack now fails validation instead of producing a false saved-night event.
- A temporary harness compiled the actual shared domain/AI source and completed **100 random games with 15,473 exact serialized save/restore checkpoints**. It also checked zero/multiple approvals, tied finalists, defense turns, final ties and abstentions, legal elimination, self/duplicate/out-of-turn rejection, legacy saves, matching Doctor saves versus unmatched kills, and private projection boundaries. No permanent test suite was added.
- Live Haiku nomination diagnosis captured a successful HTTP response containing explanatory prose followed by a correct final JSON ballot. The parser now repairs exactly one final fenced JSON response, rejects competing objects/code blocks and trailing conclusions, and validates all action targets. The surrounding text is discarded and never displayed or voiced. Invalid paid speech responses also retain their usage record. The corrected live retry advanced the same saved round with the previous five votes intact.
- Earlier live checks confirmed distinct names/identity ownership, daily shuffled order, eliminated-human spectator behavior, private notes, local paused resume, and real AI Pass backup and restore. Browser storage remains primary; cloud operations are explicit and revision-aware. Wallet balance reads and updates after paid requests worked. Audio switches/volumes and the player name persist locally.
- DeepSeek uses its documented low reasoning effort instead of the high default, with a 90-second request deadline. Earlier Day 2 live checks observed the character now named Dariush completing in 2.72 seconds; later DeepSeek decisions completed in roughly 0.8–5.9 seconds. The original timed-out provider trace was unavailable, so this identifies a plausible latency cause without asserting the exact historical server failure.
- Added ten bundled public event cues, including rooster/daybreak and owl/nightfall. Asset formats, bounds and source attribution were checked; the bridge's cancellation/unlock lifecycle received offline checks. Live TTS completed after a trusted browser gesture; an initial blocked-playback attempt showed the recovery control and did not stop gameplay. Physical-device listening quality is not established by browser status.
- The nomination spotlight and yes/no controls were reviewed on desktop and at 360×740. Vote/death announcements now also appear above the conversation. The defense label follows the character whose voice is still playing, even when the engine has advanced to the next actor. Persian rules and scripted tutorial text describe the new voting flow.
- At that deployment, tutorial narration was bundled as 40 MP3 clips generated with AI Pass `gemini-3.1-flash-tts`: 6,466 Persian characters, 610.280 seconds, and 9,808,323 bytes. All files passed hash, decode, mono, 44.1 kHz, duration, and size checks. The welcome/history and rules use the mature `Charon` narrator; scripted dialogue also uses `Kore` and `Aoede` for women and `Iapetus` and `Orus` for men. Runtime tutorial playback remains local and makes no AI call.


## Italian-style waltz and default-on music — 2026-09-13

- Added five user-provided Suno tracks to the existing background track. All six shipping assets decode as stereo 44.1 kHz MP3 and remain below the bridge's 8 MiB limit. Fresh sessions choose only from the three preferred instrumentals. Later weighted selection prevents immediate repeats, gives the lower-priority instrumental weight 24, the alternate vocal weight 12, and the rare vocal Easter egg weight 2 versus weight 100 for each preferred track. The 60.048-second edit fades for its final six seconds; measured mean level falls by 14.6 dB between seconds 50–54 and 58–60.
- Replaced the initial ambient cue with an original 80-second, 32-bar G-minor waltz: solo trumpet theme/variation/reprise, tremolo-nylon bridge, strings, accordion, and plucked bass. The exact score and local rendering are documented in [MUSIC.md](MUSIC.md). MP3 measurements show no clipping or silent 100 ms windows; subjective likeness to a film score is not established by these measurements.
- Music now defaults on. The bridge waits for an actual interaction before unlocking audio and fetching the track; welcome/tutorial activity remains silent. Explicit on/off choices use only the guarded `mafia.music.enabled.v1` localStorage boolean. Lifecycle stops and playback errors preserve that preference. TTS now has its own independent default-on preference.
- Added local playing/pending status so the UI distinguishes enabled, playing, waiting, paused, zero volume, and stopped states. Initial and muted polling synchronize the saved preference without any SDK or provider calls. Dismissed error messages stay dismissed until a new attempt.
- The final Wasm production build, bridge syntax check, Python packager check, and production packaging passed. The package contains 102 files / 43,573,786 bytes with fingerprint `83db0e762a63b6be0ee43e13ac83d7fd70b65a4cac5fdae275e9b783af8910e5`. A fresh local browser session fetched an eligible instrumental opening track and reported active music playback; neither vocal track can enter the opening candidate set.
- Published as `dpl_Ec9voxgvRJWj8D56tsFoKzBzeU9X` at both stable Vercel aliases. Vercel reports the production deployment Ready. All six public MP3 responses return HTTP 200 and `audio/mpeg`; their downloaded SHA-256 hashes match the packaged files exactly. The public bridge also matches the prepared production bridge. A fresh production browser tab reported active playback and displayed the corrected Persian status without claiming every track is instrumental. No paid model or TTS calls were made during this review. Physical mobile audio and live TTS mixing remain unverified.

## Bundled background music — 2026-09-12

- Published as `dpl_8hSBjYj8eHiUjcs4xrx1sZKTVeip` at the existing stable Vercel URL. The final Wasm build passed in 40 seconds; JavaScript bridge and Python packager syntax passed. No automated tests were added or run.
- Packaged 40 static files (16.27 MiB); anonymous production reads matched every packaged file byte-for-byte. `/audio/mafia-nocturne.mp3` returns HTTP 200, `audio/mpeg`, and revalidation headers. Entry HTML retains `no-store`.
- Added an original 71.111-second local instrumental, rendered once and bundled. No Gemini, ElevenLabs, AI Pass music generation, account upgrade, or paid call was performed. [Score, reproduction, and measured loop properties](MUSIC.md).
- Desktop browser review covered default-off controls, enabling music while speech remained disabled, 25%/50%/0% volume, tutorial return preserving settings, and the compact header indicator. The final published dialog and drawn music icon were visually checked. A local music GET succeeded after enable, with no music error shown during review.
- Source review covered independent contexts/volumes, actual-TTS ducking and restoration on all release paths, cancellation/late completion, hidden-tab and tutorial suspension, pause/resume, leave/disposal, and fixed local playback errors. Audio loading and local status reads never initialize the AI Pass SDK.
- No wallet-funded speech was generated. Actual spoken-dialogue mixing, physical mobile playback, and subjective listening quality remain unverified; numerical audio checks and UI review do not establish those.

## Wallet balance and illustrated tutorial — 2026-09-12

- Added a compact wallet-credit control in the lobby/game header. It reads the official SDK `getUserBalance()` response's `remainingBudget`, displays USD with Persian decimal digits, and preserves sub-cent values. Signed-out/unavailable states do not display a fake zero. Stale last-known values are marked, logout clears them, and balance reads have independent timeout/session/revision guards. Text/TTS output does not wait for a successful balance read.
- Added first-visit welcome/skip controls, a persistent lobby tutorial entry, four beginner cards, five scripted practice decisions, and three advanced scenarios. Only the dismissed-welcome flag is saved locally. Tutorials contain no model/TTS requests or game engine calls; named conversations, outcomes, choices, and explanations are bundled resources.
- Generated four original board-game illustrations with the built-in image tool, inspected them, and encoded 1200×800 delivery WebPs (approximately 616 KiB together). Exact prompts and paths are in `TUTORIAL_ARTWORK.md`.
- Final Kotlin/Wasm production build passed and `node --check` passed for the browser bridge. No automated tests were added or run.
- Manual desktop walkthrough completed all four beginner cards, all five practice decisions, and all three strategy scenarios. Wrong-answer feedback, choosing another response, restarting practice with cleared answers, and the explicit handoff from tutorial completion to real-game role reveal all worked. No paid generation was triggered.
- DOM checks before and after completing the whole guide found no loaded AI Pass script on the fresh tutorial origin. Opening the guide did not initialize the SDK; entering the real lobby/game did. Signed-out balance review showed the Persian connection explanation without an authorization prompt or zero amount.
- 320×568 emulation review covered the welcome choices, lesson scrolling, pinned navigation, advanced choices/feedback, and the scroll hint. The existing Compose/browser emulation scaling issue recurred after one answer; resetting/reapplying the emulated viewport restored layout. Physical-device verification remains pending.
- Published as `dpl_ANZkXwf554LtqTrMUMQYd4v4iaNf` at the existing stable Vercel address. All 38 public assets matched the packaged build. Production review confirmed the welcome, skip, credit dialog, compact mobile header, and no repeated welcome after reload. The review browser was signed out, so an authenticated live balance and balance updates after paid requests remain unverified; no paid calls were made.

## Compact lobby and fixed character models — 2026-09-12

- Removed the large sound/model panels and all model-selection routes, picker resources, and controller mutation APIs. The lobby header now opens the existing sound dialog; the dialog keeps the Persian wallet-cost notice, enable switch, and volume. Wallet disconnect remains available in the footer.
- Reduced the introduction height and placed character cards directly after it. Each card shows its fixed thinking model as a small noninteractive label, including before model discovery. Desktop review showed all six cards in the initial viewport; at 320×568, the first portraits appear without scrolling and the sound dialog remains readable.
- `CharacterModels.kt` assigns Arman/Sara/Mina to Luna, Shahab/Dariush to DeepSeek Flash, and Nika to Gemini Flash Lite. This removes the two expensive gameplay assignments while preserving fixed per-character models. Every game validates all assignments before role reveal and retains a character-ID snapshot for discussion, voting, night actions, and retries. Missing models produce a visible error without automatic substitution.
- Corrected lobby Retry to repeat the failed operation, including wallet disconnect, instead of always starting a game. Source review covered guards against duplicate actions and late completions.
- The final Kotlin/Wasm production build passed. Manual browser checks covered desktop/320×568 layout, fixed labels, relocated sound settings, enabling/disabling sound, and successful start through role reveal using the public model catalog. No paid inference or TTS calls and no automated tests were run for this change.
- Published to the same stable Vercel address as deployment `dpl_DnKeYUyyan25GoUzTLzx44EpYSu4`. The public page shows the compact controls and fixed assignments; all 31 deployed assets matched the prepared build. Existing user game tabs were not reloaded.

## Vercel production deployment — 2026-09-12

- Production deployment succeeded. The stable canonical website is [https://ai-mafia-pied.vercel.app/](https://ai-mafia-pied.vercel.app/), linked to project `ai-mafia` in scope `eiliya-s-projects`.
- `scripts/prepare-vercel.py --skip-build` successfully prepared 31 validated static files, about 14.38 MiB, from the existing Kotlin/Wasm production distribution. The package uses Build Output API version 3 and preserves the local Vercel project link. No repository source, setup recovery record, local properties, caches, or source maps are included.
- The package config sets `no-store` for `/`, `/index.html`, `/aipass-config.json`, and `/sw.js`; hashed Wasm receives `application/wasm` and immutable caching. Other app assets revalidate.
- AI Pass stable-origin migration is approved and provisioned. Public client `client_n-b2ogXewxAhsQILj4ArVw` is configured for `http://localhost:8080/` and `https://ai-mafia-pied.vercel.app/`; the setup grant reports recovery availability until `2026-10-12T20:30:02Z`, subject to revocation. The approved public configuration was repackaged and redeployed as `dpl_fbawhVAfWr5RGcLC36cbjiXqBGAX`, retaining the same canonical alias. Production wallet-connection verification remains outstanding.
- Anonymous HTTP review of all 31 initially deployed assets returned HTTP 200 and matched the reviewed package hashes. Wasm MIME was correct, and the Persian production lobby was visually verified. These checks did not initiate paid inference.
- After the final configuration deployment, the stable URL served the new public client ID matching local configuration with `no-store`. Query-bearing entry URLs, the manifest, and service worker returned HTTP 200 with the expected types/cache headers. Recovery-record and environment-file URLs returned HTTP 404. A fresh production page loaded the live AI Pass model catalog and displayed the six character assignments with GPT-5.6 Luna as the default; the page was returned to the lobby without paid requests.
- **Pending: paid end-to-end verification.** No wallet-funded production game, mixed-model round, or audible TTS round has been verified by the coding agents. No paid inference was performed for deployment verification.
- Existing live game tabs were left untouched. Fresh-tab production review must preserve those in-memory sessions. Ngrok remains an optional development route; production hosting does not depend on the local server or tunnel.

Redeploy from the repository root using the authenticated Vercel CLI and the existing `.vercel/project.json` link:

```sh
python3 scripts/prepare-vercel.py
vercel deploy --prebuilt --prod --yes --scope eiliya-s-projects
```

Run the second command only when preparation succeeds. If a current `:webApp:wasmJsBrowserDistribution` build already exists, replace the first command with `python3 scripts/prepare-vercel.py --skip-build`. Use the same shorter path after successful AI Pass provisioning changes only the public configuration. Every deployment must be repackaged so it contains the current configuration.

After redeployment, check the stable entry page, the public config, and the service worker responses without opening a paid game:

```sh
curl -I https://ai-mafia-pied.vercel.app/
curl -I https://ai-mafia-pied.vercel.app/aipass-config.json
curl -I https://ai-mafia-pied.vercel.app/sw.js
```

Each should return HTTP 200 with `Cache-Control: no-store`. Inspect the fresh-tab lobby and asset loading separately from wallet authentication and paid gameplay. For future origin changes, follow the [stable-origin AI Pass runbook](../README.md#ai-pass-setup-and-stable-origin-migration). The current approved callbacks are localhost and the stable Vercel address. Resume an existing approval with `python3 scripts/setup_aipass.py provision` rather than creating another request.

## Initial localhost/ngrok baseline — before Vercel deployment

- Kotlin/Wasm production distribution builds successfully. Remaining webpack warnings concern the standard Wasm/Skiko bundle sizes.
- JavaScript bridge syntax checked. No automated test suite was added or run.
- Manual desktop and phone browser review covered the lobby, generated images, Persian typography, RTL layout, rules, model settings, private notes, leave confirmation, role reveal and first human turn.
- The real public AI Pass catalog loads and selects GPT-5.6 Luna. At this baseline, the public OAuth client was configured for localhost:8080 and the ngrok origin recorded in `.aipass/config.json`.
- Public HTTPS application loading works after ngrok's free-tier Visit Site interstitial.
- Portrait dialogs use a single in-canvas modal to avoid a browser viewport scaling defect. Landscape game content scrolls so the chat and actions remain reachable.
- Game-engine source review covered private context filtering, votes, night actions, doctor protection, detective knowledge, dead-player exclusion, and victory transitions.
- A full wallet-funded game has not been played by the coding agents. No paid AI requests were made. Live wallet-funded verification is pending a player's connection and gameplay.

The local development server serves only production assets on loopback; ngrok forwards that port when running. No game persistence exists. PWA storage contains static assets only; wallet session custody belongs to the official AI Pass SDK.

The initial AI Pass setup used the browser SDK path and public client `client_xVghvxktxghhqtrcnBzqpQ`. That setup grant originally reported recovery availability until 2026-10-12 19:37 UTC, subject to revocation and the later hosting migration. Only public client metadata was bundled; the protected recovery record was excluded. The initial callbacks were: `http://localhost:8080/` and `https://152d-88-18-50-12.ngrok-free.app/`.

Final browser review also passed 320×568 portrait and 844×390 landscape; multiword model names now retain their reading order. No console errors or warnings were captured during the reviewed UI flows.

## AI Pass character speech — 2026-09-12

- The final Kotlin/Wasm production distribution builds, and `node --check` passes for the browser bridge. The live ngrok bridge returns HTTP 200 and matches the final source.
- Current public AI Pass catalog confirms `gpt-4o-mini-tts` with `type=audio` and `method=audio_speech`; the SDK's speech method returns an audio Blob. Six documented voices are mapped to the six characters.
- Shared narration receives only accepted public dialogue. It commits the game turn before waiting for audio, so failures cannot replay a paid dialogue request. Source review covered stop, mute, zero volume, pause, leave, abort, timeouts, late responses, and browser history restoration.
- Manual browser review covered default-off and enabled lobby controls, a volume change from 80% to 50%, the first human turn, and the in-game audio dialog. Desktop and 320×568 emulated phone layouts were inspected; no console errors or warnings were captured. A transient Compose scaling mismatch in emulation cleared after resetting/reapplying the viewport; physical mobile playback remains unverified.
- No paid speech call was made during this change. Real wallet-funded synthesis, audible Persian quality, and playback through a full talking round remain unverified. Browser UI checks and compilation do not establish these.
- The existing public OAuth client and setup grant were reused without changing callbacks or requesting new authorization. Speech charges are additional to chat charges and are reported by the AI Pass wallet, not the app's chat-token list.

## Speaker attribution correction — 2026-09-12

- Inspected the existing live discussion without restarting or submitting the user's draft. Sara's displayed first speech claimed the guest's accusation against Sara as her own. Source review found the engine supplied the correct actor/context; the prompt presented the human as `شما`, public speech only by IDs, and lacked explicit first-person ownership rules.
- Added a public `isHuman` flag and a prompt-only neutral guest name. `AgentPrompt` now creates a transcript with inline speaker names/IDs and own-speech markers, private knowledge scoped to the existing agent view, previous own-speech IDs, literal name-mention references, and a final identity reminder. Skips do not count as previous speech.
- Guidance distinguishes accusations, defenses, questions, and nested quotations. A first-time speaker cannot claim a previous accusation. The response must supply the expected speaker ID before its text is accepted or voiced. This structural validation does not guarantee the semantic perspective of every generated line.
- Kotlin/Wasm production build passed. The live ngrok app entry returns HTTP 200 and matches the final production build. Focused source review checked identity, quotations, day/night chronology, and unchanged private-information boundaries.
- Six manual semantic review cases are documented in `AI_IDENTITY_SCENARIOS.md`. They are authored examples, not generated model results. No paid AI call or automated test suite was run for this correction; model behavior on these scenarios remains unverified.
- Existing open games keep their loaded code. The updated prompt takes effect after reloading, which starts a fresh game because this prototype has no game persistence.

## Per-character thinking models — 2026-09-12

- Added catalog-validated per-character overrides and a table default. Effective choices are snapshotted at game creation and resolved by character ID for discussion, voting, and night actions; shuffled seats and roles do not change assignment. Active games cannot edit their models. Leave retains preferences, disconnect clears them, and refresh still clears in-memory configuration.
- Kotlin/Wasm production build passed. The public ngrok app entry returns HTTP 200 and matches the final build.
- Manual browser checks assigned DeepSeek V4 Flash to Arman and Claude Haiku 4.5 to Sara, then changed the default to Gemini 3.5 Flash Lite. Both explicit overrides remained intact and the other four characters inherited Gemini. Individual reset and reset-all returned the intended characters to the default.
- Search, empty-search feedback, selected-state display, and Persian model pickers were checked at desktop and 320×568 emulated phone size. No console errors or warnings were captured. These actions loaded only the public catalog and changed local settings.
- Source review confirmed unchanged private-context projection and TTS configuration, no model substitution on failures, and per-response usage retaining the actual requested model ID. No new automated test suite was added.
- No paid mixed-model game was run. Catalog presence and UI checks do not establish every provider's structured-response quality, latency, or compatibility. Existing SDK/provider behavior still applies, including provider-specific reasoning defaults and sampling-parameter handling.
